import React from 'react';
import BoardFlow from './BoardFlow/BoardFlow';

const Board = () => {
    return (
        <div className={"board-flow"}>
            <BoardFlow />
        </div>
    );
};

export default Board;